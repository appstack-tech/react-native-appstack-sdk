#pragma once

// Preserve the CocoaPods public header without moving it away from the pod's
// source glob. SwiftPM exposes this namespaced forwarding header instead.
#import "../../AppstackBridge.h"
